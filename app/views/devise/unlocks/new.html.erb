<h2><section class = "page_navi">ロック解除を行う</h2></section>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <div class = "b_field"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
  </div>

  <div class="actions">
    <%= f.submit "ロック解除" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
